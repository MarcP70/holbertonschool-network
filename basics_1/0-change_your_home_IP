#!/usr/bin/env bash
# Add or update entries to the hosts file

# Change localhost resolution to both IPv4 and IPv6
sed -i -E 's/127\.0\.0\.1/127.0.0.2/g' /etc/hosts
sed -i -E 's/::1/::2/g' /etc/hosts

# Add a new entry for facebook.com
bash -c 'echo "8.8.8.8 facebook.com" >> /etc/hosts'
